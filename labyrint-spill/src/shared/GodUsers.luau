--!strict
-- Spillere med "gudemodus": fly + gå gjennom vegger + udødelig. Kun for
-- navngitte spillere (match på Roblox-brukernavn). Serveren gir udødelighet KUN
-- til disse; fly + noclip gjøres klient-side (GodModeClient). Legg til flere
-- brukernavn ved behov. Bytt til userId-sjekk hvis noen bytter navn.

return {
	["MioSpille"] = true,
}
